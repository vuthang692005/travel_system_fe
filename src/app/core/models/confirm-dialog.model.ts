export interface ConfirmDialogData {
  icon?: string;
  title: string;
  message: string;
  cancelText?: string;
  confirmText?: string;
  confirmColor?: string; // Ví dụ: 'bg-red-600' hoặc 'bg-blue-600'
}
